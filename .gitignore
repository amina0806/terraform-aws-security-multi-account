# ---- Terraform (keep the lockfile tracked: .terraform.lock.hcl) ----
.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
crash.log
crash.*.log

# Terraform plan outputs (binary + JSON you generate for OPA)
*.tfplan
**/plan.tfplan
**/plan.json

# Local override files (never commit)
override.tf
override.tf.json

# Sensitive / local
*.tfvars
*.tfvars.json
*.pem
*.key

# Optional: temp PaC work
policies-as-code/opa-wip/

# IDE / OS noise
.vscode/
.idea/
.DS_Store
